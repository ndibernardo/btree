[package]
name = "btree"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
proptest = { version = "1.11.0", default-features = false, features = ["std"] }
